/**
 * HTTP protocol is used to get web pages hosted on the web
 */
/**
 * HTML document format for web pages
 */

/**
 * tags are inside <>
 * for actual `<` character ==> use &lt;
 * for `>` ==> &gt;
 * for `&` ==> &amp;
 * for `""` ==> &quot;
 * rest of the stuff from the chapter is trivial
 */
